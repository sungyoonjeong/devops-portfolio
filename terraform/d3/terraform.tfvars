# =============================================================================
# d3/terraform.tfvars  —  변수 실제 값 (terraform 이 자동 로드)
# =============================================================================

project    = "portfolio" # 이름 접두사
aws_region = "us-east-1" # LocalStack 리전
# ami_id 는 variables.tf 의 기본값(가짜 AMI) 그대로 사용
