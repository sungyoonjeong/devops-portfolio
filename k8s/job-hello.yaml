apiVersion: batch/v1
kind: Job
metadata:
  name: job-hello
spec:
  backoffLimit: 2
  template:
    spec:
      containers:
      - name: hello
        image: busybox:1.36
        command: ["sh", "-c", "echo hello from job; sleep 15"]
      restartPolicy: Never
